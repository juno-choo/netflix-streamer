/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This tells Tailwind to scan all JS/JSX files in src/
  ],
  theme: {
    extend: {}, // We’ll customize this later if needed
  },
  plugins: [],
}